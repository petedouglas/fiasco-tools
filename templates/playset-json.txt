{
	playset :
	{
		title: '[% title %]',
		credits: 
		{
			author : '[% credits.author %]'
		},
		movie_night :
		[[% FOREACH movie = movie_night %]
			{ title : '[% movie.title %]', imdb_code : '[% movie.code %]' }[% IF (loop.count != loop.size) %],[% END %][% END %]
		]
	}
}