{
  "playset" :
  {
    "title" : "[% title %]",
    "credits" : 
    {
      "authors" : [ [% FOREACH author = credits.authors %]"[% author %]"[% IF (loop.count != loop.size) %], [% END %][% END %] ],
      "editors" : [ [% FOREACH editor = credits.editors %]"[% editor %]"[% IF (loop.count != loop.size) %], [% END %][% END %] ],
      "licence" : ""
    },
    "movie_night" :
    [[% FOREACH movie = movie_night %]
      { "title" : "[% movie.title %]" }[% IF (loop.count != loop.size) %],[% END %][% END %]
    ],
    "score" :
    {
      "title" : "[% score.title %]",
      "paragraphs" : [[% FOREACH paragraph = score.paragraphs %]
"[% paragraph %]"[% END %]],
      "tagline" : "[% score.tagline %]"
    },
    "relationships" : [[% INCLUDE section.txt section=relationships %]
    ],
    "needs" : [[% INCLUDE section.txt section=needs %]
    ],
    "locations" : [[% INCLUDE section.txt section=locations %]
    ],
    "objects" : [[% INCLUDE section.txt section=objects %]
    ]
  }
}